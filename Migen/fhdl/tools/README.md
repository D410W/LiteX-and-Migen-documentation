# tools

This module contains utility functions for inspecting, querying, and manipulating Migen AST fragments (finding all clock domains in a fragment, replacing signals, walking the AST tree).
It is mostly used by advanced Migen users and other libraries that use Migen as a dependency (LiteX, for example).